{
  "name": "automated-test",
  "version": "0.1.0",
  "private": true,
  "description": "SRS 頁面管理功能 — 自動化驗收試作",
  "scripts": {
    "dev": "http-server app -p 5173 -c-1 --silent",
    "sass": "sass --watch app/assets/scss/main.scss:app/assets/css/main.css",
    "test": "playwright test",
    "test:contract": "playwright test tests/contract"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "http-server": "^14.1.1",
    "sass": "^1.103.1"
  }
}
